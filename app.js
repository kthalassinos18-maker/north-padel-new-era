const bookingButton=document.getElementById('bookingButton');const bookingPopover=document.getElementById('bookingPopover');const closeButtons=document.querySelectorAll('[data-close-booking]');function setBooking(open){bookingPopover.classList.toggle('open',open);bookingPopover.setAttribute('aria-hidden',String(!open))}bookingButton?.addEventListener('click',()=>setBooking(true));closeButtons.forEach(button=>button.addEventListener('click',()=>setBooking(false)));document.addEventListener('keydown',event=>{if(event.key==='Escape')setBooking(false)});
